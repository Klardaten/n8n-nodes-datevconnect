{
  "name": "n8n-datevconnect",
  "version": "1.0.0",
  "description": "n8n community node for DATEVconnect API via Klardaten Gateway",
  "keywords": [
    "n8n",
    "n8n-community-node-package",
    "klardaten",
    "datev",
    "datevconnect",
    "master-data"
  ],
  "n8n": {
    "nodes": [
      "dist/nodes/MasterData/MasterData.node.js",
      "dist/nodes/Accounting/Accounting.node.js",
      "dist/nodes/DocumentManagement/DocumentManagement.node.js",
      "dist/nodes/IdentityAndAccessManagement/IdentityAndAccessManagement.node.js"
    ],
    "credentials": [
      "dist/credentials/DatevConnectApi.credentials.js"
    ]
  },
  "type": "module",
  "module": "index.ts",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist && mkdir -p dist/nodes dist/credentials",
    "build": "bun run clean && sh -c 'NODES=$(find nodes -name \"*.node.ts\" -print) && [ -z \"$NODES\" ] || bun build $NODES --outdir dist/nodes --format cjs --target=node --external n8n-workflow --external n8n-core' && sh -c 'CREDS=$(find credentials -name \"*.credentials.ts\" -print) && [ -z \"$CREDS\" ] || bun build $CREDS --outdir dist/credentials --format cjs --target=node --external n8n-workflow --external n8n-core' && sh -c 'SERVICES=$(find src/services -name \"*.ts\" -print) && [ -z \"$SERVICES\" ] || bun build $SERVICES --outdir dist/src/services --format cjs --target=node' && cp nodes/klardaten.svg dist/nodes/ && bun run build:types",
    "build:types": "tsc -p tsconfig.declarations.json",
    "watch": "bun run clean && sh -c 'NODES=$(find nodes -name \"*.node.ts\" -print) && [ -z \"$NODES\" ] || bun build $NODES --outdir dist/nodes --format cjs --target=node --external n8n-workflow --external n8n-core --watch' & sh -c 'CREDS=$(find credentials -name \"*.credentials.ts\" -print) && [ -z \"$CREDS\" ] || bun build $CREDS --outdir dist/credentials --format cjs --target=node --external n8n-workflow --external n8n-core --watch'",
    "lint": "eslint --ext .ts,.tsx --max-warnings 0 .",
    "test": "bun test",
    "typecheck": "bun x tsc --noEmit"
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts",
    "dist/**/*.d.ts.map",
    "dist/nodes/klardaten.svg"
  ],
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/node": "24.9.1",
    "@typescript-eslint/eslint-plugin": "^8.46.2",
    "@typescript-eslint/parser": "^8.46.2",
    "eslint": "^8.57.1",
    "nock": "14.0.10",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "n8n-core": "1.114.1",
    "n8n-workflow": "1.114.0"
  }
}
