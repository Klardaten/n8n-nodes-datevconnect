{
  "name": "@klardaten/n8n-nodes-datevconnect",
  "version": "1.0.2",
  "description": "n8n community node for DATEVconnect API via Klardaten Gateway",
  "author": "Klardaten <info@klardaten.com> (https://klardaten.com)",
  "homepage": "https://github.com/Klardaten/n8n-nodes-datevconnect#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Klardaten/n8n-nodes-datevconnect.git"
  },
  "bugs": {
    "url": "https://github.com/Klardaten/n8n-nodes-datevconnect/issues"
  },
  "license": "MIT",
  "keywords": [
    "n8n",
    "n8n-community-node-package",
    "klardaten",
    "datev",
    "datevconnect",
    "master-data",
    "document-management",
    "iam"
  ],
  "n8n": {
    "nodes": [
      "dist/nodes/MasterData/MasterData.node.js",
      "dist/nodes/Accounting/Accounting.node.js",
      "dist/nodes/DocumentManagement/DocumentManagement.node.js",
      "dist/nodes/IdentityAndAccessManagement/IdentityAndAccessManagement.node.js"
    ],
    "credentials": [
      "dist/credentials/DatevConnectApi.credentials.js"
    ]
  },
  "files": [
    "dist/credentials",
    "dist/nodes",
    "README.md"
  ],
  "type": "commonjs",
  "scripts": {
    "dev": "bun watch",
    "clean": "rm -rf dist && mkdir -p dist/nodes dist/credentials",
    "build": "bun run clean && sh -c 'NODES=$(find nodes -name \"*.node.ts\" -print) && [ -z \"$NODES\" ] || bun build $NODES --outdir dist/nodes --format cjs --target=node --external n8n-workflow --external n8n-core' && sh -c 'CREDS=$(find credentials -name \"*.credentials.ts\" -print) && [ -z \"$CREDS\" ] || bun build $CREDS --outdir dist/credentials --format cjs --target=node --external n8n-workflow --external n8n-core' && node scripts/copy-codex.js && cp nodes/klardaten.svg dist/nodes/",
    "watch": "bun run build && sh -c 'NODES=$(find nodes -name \"*.node.ts\" -print) && [ -z \"$NODES\" ] || bun build $NODES --outdir dist/nodes --format cjs --target=node --external n8n-workflow --external n8n-core --watch' & sh -c 'CREDS=$(find credentials -name \"*.credentials.ts\" -print) && [ -z \"$CREDS\" ] || bun build $CREDS --outdir dist/credentials --format cjs --target=node --external n8n-workflow --external n8n-core --watch'",
    "lint": "eslint --ext .ts,.tsx --max-warnings 0 .",
    "test": "bun test",
    "typecheck": "bun x tsc --noEmit",
    "copy:codex": "node scripts/copy-codex.js",
    "prepublishOnly": "bun run lint && bun run typecheck && bun test && bun run build"
  },
  "engines": {
    "node": ">=18.18",
    "npm": ">=9.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/node": "24.9.1",
    "@typescript-eslint/eslint-plugin": "^8.46.2",
    "@typescript-eslint/parser": "^8.46.2",
    "eslint": "^8.57.1",
    "nock": "14.0.10",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "n8n-core": "1.114.1",
    "n8n-workflow": "1.114.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
